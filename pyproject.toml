[build-system]
requires = ["setuptools>=77,<85", "wheel>=0.45,<1"]
build-backend = "setuptools.build_meta"

[project]
name = "f2media-fnos"
version = "0.1.7"
description = "fnOS multi-platform media downloader with F2 as the primary engine"
requires-python = ">=3.12"
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "cryptography>=43,<47",
  "httpx>=0.27,<1",
  "fastapi-mcp==0.4.0",
  "parse-video-py==0.0.3",
]

[project.optional-dependencies]
engines = [
  "f2==0.0.1.7",
  "gallery-dl==1.32.9",
]
dev = ["pytest>=8,<10", "ruff>=0.8"]

[project.scripts]
f2media = "f2media.main:run"

# Explicitly package only the application. fnos/, scripts/ and tests/ are
# repository/build assets and must never be auto-discovered as Python packages.
[tool.setuptools.packages.find]
where = ["."]
include = ["f2media", "f2media.*"]
exclude = ["fnos*", "scripts*", "tests*"]
namespaces = false

[tool.setuptools.package-data]
f2media = ["static/*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
